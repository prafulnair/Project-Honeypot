A simple Keylogger Scripted in Python. This is to be enabled when honeypot is setup. Whenever the honeypot is accessed, the keys entered by the bad actor are stored onto a file, which can be 
parsed to extract information. 

Modules Required : 
- pynput - > keyboard
- twisted.python -> log
- sys
